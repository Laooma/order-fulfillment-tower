# CLAW — 经验教训与纠错日志

## 使用规则

### 读取（执行 skill 前必须）
每次执行 skill 相关任务前，**必须先通读本文件中的「经验记录」章节**，将过往的纠错经验作为本次输出的校验依据。如果发现当前输出与历史教训矛盾，应主动修正后再给出最终结果。

### 写入（用户纠错后必须）
当用户指出分析/输出有误，经排查找到根因并给出正确答案后，**必须在「经验记录」中新增一条**，格式如下：

```markdown
### YYYY-MM-DD：<一句话标题>

**问题**：用户反馈了什么错误？
**根因**：为什么错了？
**正确做法**：最终的正确方案是什么？
**适用场景**：什么类型的 skill / 任务需要参考此条？
```

只记**有复用价值的教训**——同样的错误下次可能再犯的才记。一次性的拼写错误、临时的格式调整不记。

---

## 经验记录

> 暂无记录。每次用户纠错后，由智能体在此追加。
