      26 +### 2026-05-25：何时创建分析任务 vs 何时使用 A2UI 渲染                                                                                                                         
      27 +                                                                                                                                                                               
      28 +**问题**：智能体在处理分析请求时，不清楚应该走「分析任务」流程还是直接用「A2UI 组件」渲染。                                                                                    
      29 +**根因**：分析任务是后端异步处理，A2UI 是前端结构化渲染，两者的触发时机不同，但界限模糊。                                                                                      
      30 +**正确做法**：                                                                                                                                                                 
      31 +- **走分析任务**：用户请求涉及多订单批量分析、需要后端 Agent 处理（如履约分析、齐套检查）、需要生成待办清单的场景。调用 `api.analysis.create()` 创建任务，由后端 Agent 异步处理
         +。                                                                                                                                                                             
      32 +- **走 A2UI 渲染**：前端已有分析结果数据，需要以结构化卡片/表格/看板形式展示。A2UI 消息通过 WebSocket 推送或内嵌在 analysis full 结果中，由 `A2uiRenderer` 组件渲染。          
      33 +**适用场景**：所有涉及分析与展示的 skill（订单履约分析、齐套分析、FAT计划等）。                                                                                                
      34 +                                                                                                                                                                               
      35 +### 2026-05-25：待办清单生成卡死 + 字段映射错误                                                                                                                                
      36 +                                                                                                                                                                               
      37 +**问题**：生成待办清单时前端卡死，且合同编号显示错误的字段名（如显示 `contract_number` 而非格式化后的值）。                                                                    
      38 +**根因**：                                                                                                                                                                     
      39 +1. 待办生成接口返回数据中 `contractNumber` 字段来自后端 `contract_number`，但前端 TodoWidget 期望的字段名不一致导致死循环。                                                    
      40 +2. A2UI 组件未做防御性渲染，遇到异常数据直接崩溃。                                                                                                                             
      41 +**正确做法**：                                                                                                                                                                 
      42 +- 后端 API 响应中统一使用 camelCase 字段名（`contractNumber` 而非 `contract_number`），在 `mapRow` 中完成转换。                                                                
      43 +- A2UI 组件必须增加 `try/catch` 防御，遇到无法渲染的数据类型时 fallback 显示原始文本，不可让整个页面崩溃。                                                                     
      44 +- 批量操作（如生成待办）需加 loading 状态和超时机制，防止无限等待。                                                                                                            
      45 +**适用场景**：待办生成、A2UI 组件开发、前后端数据映射。                                                                                                                        
      46 +                                                                                                                                                                               
      47 +### 2026-05-25：筛选计数硬编码导致假数据                                                                                                                                       
      48 +                                                                                                                                                                               
      49 +**问题**：任务列表侧栏筛选器中"全部合同""全部类型"等选项的计数固定显示 42，与真实任务数不一致。                                                                                
      50 +**根因**：开发阶段使用了硬编码的 `count: 42` 作为占位符，后续忘记替换为真实数据统计。                                                                                          
      51 +**正确做法**：                                                                                                                                                                 
      52 +- 所有统计数字必须从真实数据源计算（如 `allMockTasks.length`、`tasks.filter(...).length`）。                                                                                   
      53 +- 禁止在 UI 中使用魔术数字占位，如需占位用 `0` 或 `—` 明确表示"暂无数据"。                                                                                                     
      54 +- Review 阶段重点检查筛选、统计、分页相关的数字是否来自真实数据。                                                                                                              
      55 +**适用场景**：列表页、筛选面板、统计卡片、分页组件。                                                                                                                           
      56 +                                                                                                                                                                               
      57 +### 2026-05-25：页面圆角化时背景色被遮挡                                                                                                                                       
      58 +                                                                                                                                                                               
      59 +**问题**：分析任务详情页做圆角化后，四个角出现白色，与页面底色不一致。                                                                                                         
      60 +**根因**：外层容器没有设置背景色（`transparent`），body/html 的默认白色透过圆角显露。排水渠（`.panel-resize-handle`）的背景色也没有与整体协调。                                
      61 +**正确做法**：                                                                                                                                                                 
      62 +- 圆角容器必须设置与页面底色一致的 `background`（如 `bg-bg`），否则透明背景会让底层颜色透出。                                                                                  
      63 +- 分割线/排水渠应使用 `transparent` 背景，让两侧面板的颜色自然填充。                                                                                                           
      64 +- 做圆角化改造时，需要同时检查：容器背景、子元素 overflow 裁剪、分割线颜色协调。                                                                                               
      65 +**适用场景**：UI 圆角化改造、面板布局、分割线样式。                                                                                                                            
      66 +                                                                                                                                                                               
      67 +### 2026-05-25：JSON 配置驱动渲染时字段名映射                                                                                                                                  
      68 +                                                                                                                                                                               
      69 +**问题**：任务详情 Meta 面板的 JSON 配置中，`dataKey` 指向的字段名必须与 API 返回的数据字段严格一致，否则显示 `—`。                                                            
      70 +**根因**：TaskDetailLayout 中通过 `enriched[dataKey]` 取值，`enriched` 由 `task` 展开得来。如果字段名不匹配（如 API 返回 `contractId` 但配置写 `contract_id`），取值结果为 `und
         +efined`。                                                                                                                                                                      
      71 +**正确做法**：                                                                                                                                                                 
      72 +- 新增 JSON 配置字段前，先确认 API 返回的实际字段名（查看 types 定义 + 后端 mapRow）。                                                                                         
      73 +- 对于可选字段（如 `supervisor`、`skillName`），在配置中设置 `static` 兜底值（如 `'无skill'`），避免空值显示 `—`。                                                             
      74 +- 建议在 types 中为所有可能用到的字段显式声明类型。                                                                                                                            
      75 +**适用场景**：JSON 配置驱动的 UI 渲染、Meta 面板开发、前后端字段对接。      