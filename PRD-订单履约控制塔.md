# 订单履约控制塔 — 产品需求文档（PRD）

> **文档版本**: v1.0  
> **编写日期**: 2026/06/03  
> **产品名称**: 订单履约控制塔 (Order Fulfillment Control Tower)

---

## 一、产品概述

### 1.1 产品定位

订单履约控制塔是一套面向制造业/工程类企业的**订单全生命周期履约管理平台**，覆盖从销售订单签订、生产排期、物料齐套、发货签收到收入确认的全链条管理。

核心价值主张：**以 AI 智能体驱动，将订单履约数据转化为可操作的决策洞察**。

### 1.2 目标用户

| 角色 | 典型用户 | 核心需求 |
|------|---------|---------|
| 销售经理 | 销售团队负责人 | 查看订单履约进度、发货率、签收率 |
| 履约专员 | 订单执行人员 | 跟踪具体订单的交付状态、处理异常 |
| 物料采购员 | 采购部门 | 查看物料短缺情况、齐套分析 |
| 项目经理 | PMO | 跨合同跨项目的履约全景监控 |
| 系统管理员 | IT/运维 | 系统配置、用户权限、模型接入管理 |

### 1.3 产品边界

**本系统负责**:
- 销售订单与机柜包的履约状态查询与筛选
- 合同→设备→包→物料的 BOM 层级结构与供需平衡
- AI 智能体驱动的履约分析、看板生成、风险预警
- 任务管理（Agent任务 / 决策任务 / 手工任务）
- 定时任务、通知分发（邮件/飞书/企微）
- 多模型接入（DeepSeek / 豆包 等）

**本系统不负责**:
- ERP 系统的原生订单创建（订单数据由系统导入或 mock）
- 财务结算与发票管理
- 生产制造执行（MES 层）

---

## 二、用户角色与权限体系

### 2.1 角色定义

| 角色 | 权限范围 | 说明 |
|------|---------|------|
| 系统管理员 | 全部权限 | 拥有所有菜单、操作、数据、技能权限 |
| 普通用户 | 受控权限 | 按角色分配菜单可见性和操作权限 |

### 2.2 权限模型 (RBAC)

- **菜单权限**: 控制左侧导航和系统设置中的菜单项可见性
- **操作权限**: 控制增删改查及特定业务操作（如 `view_orders`、`assign_todos`、`manage_skills`）
- **数据权限**: 按组织架构进行数据范围隔离
- **技能权限**: 控制 AI 智能体技能的使用权限

### 2.3 组织架构

支持树形组织结构（`orgs` 表），用户归属到具体组织节点，数据权限可按组织范围配置。

---

## 三、核心功能模块

### 3.1 订单履约看板（首页）

**路由**: `/`  
**页面组件**: `HomePage`

首页是用户进入系统后的主工作台，提供双视图切换：

#### 3.1.1 销售订单视图

以表格形式展示全部销售订单，默认 60 条种子数据。

**筛选条件（11项）**:
- 销售员（文本搜索）
- 品牌主体（文本搜索）
- 签收状态：未签收 / 部分签收 / 全部签收
- 出库状态：待出库 / 已出库 / 部分出库
- 是否异常：正常 / 异常
- 客户主体：华东区客户 / 华南区客户 / 华北区客户
- 采购员（文本搜索）
- 供应商（文本搜索）
- 发货计划：本周 / 下周 / 本月
- 发货状态：未发货 / 已发货 / 部分发货
- 支持自定义配置筛选字段

**列表字段（13列）**:
`#` | 订单编号 | 订单主体 | 品牌主体 | 销售员 | 下单日期 | 交货期 | 发货方式 | SKU数 | 订单金额 | 签收率 | 发货进度 | 异常状态 | 操作

**功能按钮**:
- 「加入对话」: 将单个订单加入 AI 对话上下文
- 「批量添加至对话」: 勾选多个订单批量加入对话
- 「筛选」/「排序」/「列设置」: 表格自定义
- 「配置筛选字段」: 自定义筛选条件显示

#### 3.1.2 机柜包视图

切换至机柜包（Cabinet Package）维度，展示机柜级别的包列表，支持按状态、工厂、发货状态、客户筛选。

### 3.2 AI 对话与智能分析

这是系统的**核心交互层**，打通了"数据→AI→决策"的完整链路。

#### 3.2.1 聊天面板（ChatPanel）

**位置**: 页面右侧固定面板，可拖拽调整宽度

**核心能力**:
- **文本对话**: 用户输入自然语言，AI 智能体回复
- **上下文引用**: 将订单数据加入对话作为分析上下文
- **技能选择**: 从 33 个预置技能中选择智能体
- **自动分配**: 系统根据问题类型自动匹配智能体
- **多模型切换**: DeepSeek-Chat / DeepSeek-R1 / DeepSeek-V4 / 豆包 2.0
- **电子宠物**: 用户可领养虚拟宠物（Trump / Tux / Dario 等），显示在对话区增加趣味性

#### 3.2.2 技能系统（33个技能）

技能是 AI 智能体的专业化封装，每个技能定义了特定的能力边界：

**业务分析类（6个）**:
| 技能 | 功能 |
|------|------|
| 订单履约分析系统 | 履约全过程的时效诊断、异常识别、风险评级和报告生成 |
| 产品型销售订单履约看板 | 按时间段筛选订单，生成统计卡片+图表+表格的看板 |
| 产品型销售订单确认收入智能体 | 产品销售订单的收入确认与履约分析 |
| 工程项目型销售订单确认收入智能体 | 工程类项目订单的履约管理与收入确认 |
| 订单风险预警助手 | 自动识别异常订单、逾期订单和高风险交易 |
| 合同风险预警助手 | 分析合同履约风险并预警 |

**物料与供应链类（3个）**:
| 技能 | 功能 |
|------|------|
| 预缺分析与齐套检查助手 | 合同/包内物料齐套分析，生成每日供需平衡报表和图表 |
| 供需平衡报表助手 | 物料供需数据分析与预警报表 |
| 预缺分析及齐套助手智能体 | 物料预缺与齐套进度分析 |

**任务与通知类（3个）**:
| 技能 | 功能 |
|------|------|
| 任务督办助手 | 定时或手动触发，汇总待办任务按人员分发飞书通知 |
| 逾期督办助手 | 每日9:00自动催办逾期任务并抄送上级 |
| 待办任务邮件通知助手 | 查询未完成任务，生成邮件通知并抄送 |

**飞书集成类（18个）**:
包括 lark-base（多维表格）、lark-doc（云文档）、lark-sheets（电子表格）、lark-calendar（日历）、lark-im（即时通讯）、lark-mail（邮箱）、lark-task（任务）、lark-wiki（知识库）、lark-vc（视频会议）、lark-minutes（妙记）、lark-contact（通讯录）、lark-drive（云空间）、lark-event（事件订阅）、lark-whiteboard（白板）等

**工具类（3个）**:
| 技能 | 功能 |
|------|------|
| 分析助手 | 将分析结果以可视化看板、图表、仪表盘形式呈现 |
| 分析任务管理器 | 创建和管理数据分析任务 |
| Skill 创建助手 | 通过对话式交互生成新的 Skill 定义 |
| Hook 创建助手 | 帮助设计 Hook 脚本 |
| 定时任务编辑助手 | 创建和编辑定时任务脚本与 cron 表达式 |
| FAT计划助手 | 出厂验收测试计划管理与进度跟踪 |

#### 3.2.3 Agent 对话流程

```
用户消息 → WebSocket (ws://localhost:3002/ws/agent)
  → Agent Loop (agentLoop.ts) 
  → LLM 推理（DeepSeek / 豆包）
  → 工具调用 → MCP Pool 工具执行
  → 流式返回结果 → 前端实时渲染
  → 完成后自动保存会话到 SQLite
```

关键特性:
- **多轮对话**: 完整会话历史保留
- **任务列表**: AI 可创建结构化 Todo 列表追踪复杂分析
- **工具调用**: 支持 MCP 协议的工具调用（4个内置工具）
- **会话管理**: 支持新建对话、历史对话回溯
- **中断支持**: 可随时中止正在执行的 Agent 任务

### 3.3 A2UI 可视化看板

**路由**: `/a2ui`  
**核心组件**: `A2uiRenderer`

A2UI 是 Agent-to-UI 的缩写，是 AI 智能体直接生成可视化界面的协议层。

#### 3.3.1 工作流程

```
AI 分析完成 → 生成 a2ui_surface 消息（JSON） 
→ WebSocket 推送到前端 
→ a2uiStore.setSurface() 写入状态
→ 页面自动跳转至 /a2ui
→ A2uiRenderer 根据消息类型渲染对应组件
```

#### 3.3.2 支持的组件类型

| 类型 | 说明 | 示例 |
|------|------|------|
| `title` | 页面标题 | "订单履约看板 — HT202546001 & HT202512002" |
| `heading` | 章节标题 | H1/H2 级别标题 |
| `stats_cards` | 统计卡片组 | 订单总数、总金额、平均发货率等 |
| `chart` | 图表 | 柱状图、折线图、对比图等 |
| `table` | 数据表格 | 订单明细表 |
| `text` | 文本块 | 描述性文字 |
| `badge` | 状态标签 | 正常/异常标签 |

#### 3.3.3 典型看板输出

一次完整的 A2UI 看板包含:
1. **头部标题**: 看板名称与时间范围
2. **统计卡片行**: 5个核心指标卡片
3. **图表区域**: 多组图表（按维度对比）
4. **明细表格**: 完整数据列表
5. **数据自动持久化**: 分析结果保存到 `analysis_tasks` 表

### 3.4 任务管理系统

**路由**: `/tasks`（任务列表）、`/task/agent/:id`、`/task/decision/:id`、`/task/manual/:id`

#### 3.4.1 任务分类

| 类型 | 说明 | 详情页 |
|------|------|--------|
| **Agent 任务** | AI 智能体自动执行的分析/数据处理任务 | `TaskAgentPage` |
| **决策任务** | 需要人工决策选择的任务（提供方案对比） | `TaskDecisionPage` |
| **手工任务** | 需要人工操作执行的待办事项 | `TaskManualPage` |

#### 3.4.2 任务属性

- **业务分类**: 发货任务 / 入库任务 / 合同确认 / 异常处理
- **优先级**: 高 / 中 / 低
- **状态流转**: 待开始 → 进行中 → 已完成 / 逾期
- **责任人 & 监督人**: 每项任务指派明确的执行人和上级

#### 3.4.3 任务列表功能

- 按业务分类和类型筛选
- 关键词搜索
- 分页浏览
- 点击任务跳转到对应的详情处理页
- 左侧常驻聊天面板（可收起）

#### 3.4.4 决策任务详情页

提供多方案对比视图，每个方案包含:
- 方案名称与描述
- 预计耗时
- 风险等级（低/中/高）
- 成本估算
- 用户可提交意见备注

### 3.5 分析任务与历史

**路由**: `/analysis/:taskId?`（分析结果详情）、`/history`（历史分析列表）

- 每次 AI 分析任务自动记录
- 支持查看历史分析任务列表
- 点击可查看完整的分析结果（包含问题分类、待办、交付表等）
- 分析结果自动关联订单、问题卡片

### 3.6 系统设置

**路由**: `/settings`

系统设置包含 12 个管理面板，按权限可见：

| 面板 | 功能 |
|------|------|
| 大模型接入点 | 管理 LLM Provider 配置（API Key、URL、模型列表） |
| Skill 管理 | 创建/编辑/删除技能，配置图标、颜色、Reference 文档 |
| 经验教训 | 管理 claw-code 的经验教训知识库 |
| Hook 管理 | 管理自动化 Hook 脚本 |
| MCP 管理 | 管理 MCP Server 连接配置 |
| 插件管理 | 管理系统插件 |
| 组织机构 | 管理树形组织结构 |
| 用户管理 | 用户的增删改查、启停用、角色分配 |
| 角色与权限 | 角色定义、菜单+操作+数据+技能权限分配 |
| 定时任务 | 管理 cron 定时任务（创建/编辑/手动触发） |
| 通知管理 | 通知渠道配置 + 通知模板 + 发送日志 |
| Tool 管理 | 管理 Agent 可用的工具配置 |

### 3.7 通知系统

#### 3.7.1 通知渠道

支持 4 种渠道类型:
- **邮件 (email)**: SMTP 邮件发送
- **企业微信 (wecom)**: 企微机器人 Webhook
- **飞书 (feishu)**: 飞书机器人 Webhook
- **飞书应用 (feishu_app)**: 飞书应用消息推送

#### 3.7.2 通知模板

- 支持变量替换的消息模板
- 模板可编辑，支持 Markdown 格式
- 记录完整的发送日志（成功/失败/详情）

### 3.8 定时任务

内置 cron 调度引擎，支持的定时任务:
- **任务督办助手**: 每日 9:00 汇总待办按人员分发通知
- 用户可自定义 cron 表达式创建定时任务
- 支持手动触发执行
- 任务执行历史可追溯

---

## 四、业务数据模型

### 4.1 合同→设备→包→物料 层级关系

```
biz_contracts (合同)
  └── biz_devices (设备，1合同:N设备)
       └── biz_packages (包，1设备:N包)
            └── biz_materials (物料，1包:N物料)
                 └── biz_material_daily_balance (每日供需平衡)
```

### 4.2 种子数据规模

| 实体 | 数量 |
|------|------|
| 合同 | 5 |
| 设备 | 10-15 |
| 包 | 20-75 |
| 物料 | 100-750 |
| 每日平衡记录 | 数千条（按日期生成） |
| 销售订单 | 60 |
| 供应商 | 10+ |

### 4.3 物料状态

- **齐套状态**: 已齐套 / 部分齐套 / 未齐套
- **短缺计算**: `shortage_qty = max(0, required_qty - current_stock - in_transit)`
- **库存模拟**: ~30% 概率出现短缺（严重短缺15% + 部分短缺20%）

---

## 五、技术架构

### 5.1 系统拓扑

```
┌─────────────────────────────────────────────────┐
│                    Browser                       │
│  React + Vite + Tailwind CSS + Zustand          │
│  Port: 5173 (dev)                               │
└──────────┬────────────────────┬─────────────────┘
           │ HTTP               │ WebSocket
           ▼                    ▼
┌──────────────────┐  ┌──────────────────────────┐
│   Backend        │  │   Agent Service           │
│   Express 5      │  │   Express 5 + WebSocket   │
│   Port: 3001     │  │   Port: 3002              │
│                  │  │                           │
│   Routes:        │  │   Routes:                 │
│   - /api/orders  │  │   - /skills               │
│   - /api/tasks   │  │   - /models               │
│   - /api/auth    │  │   - /hooks                │
│   - /api/rbac    │  │   - /mcp                  │
│   - /api/analysis│  │   - /plugins              │
│   - /api/claw    │  │   - /cron-tasks           │
│   - /ws/chat     │◄─┤   - /ws/agent             │
│   SQLite (WAL)   │  │   MCP Pool (4 clients)    │
└──────────────────┘  └──────────────────────────┘
```

### 5.2 核心数据库表

| 表名 | 用途 |
|------|------|
| `users` / `roles` / `user_roles` / `orgs` / `menus` / `role_permissions` | RBAC 权限模型 |
| `biz_contracts` / `biz_devices` / `biz_packages` / `biz_materials` / `biz_material_daily_balance` | 业务 BOM 数据 |
| `analysis_tasks` / `analysis_orders` / `analysis_problem_categories` / `analysis_problems` / `analysis_card_details` / `analysis_todos` / `analysis_delivery_tables` | AI 分析结果 |
| `chat_sessions` / `chat_messages` | 对话历史 |
| `notification_channels` / `notification_templates` / `notification_logs` | 通知系统 |

### 5.3 数据库引擎

- **SQLite** + WAL 模式 + 外键约束
- 自动 schema 迁移（`runMigrations`）
- 首次启动自动种子数据（`seedBizData`）
- 数据文件: `backend/data/analysis.db`

### 5.4 前端技术栈

- **框架**: React 19 + TypeScript 6
- **构建**: Vite 8
- **样式**: Tailwind CSS 4
- **状态管理**: Zustand 5（authStore、chatStore、tabStore、a2uiStore、petStore）
- **路由**: React Router 7
- **图标**: Lucide React
- **Markdown**: marked + Prism.js（代码高亮）
- **WebSocket**: 原生 WebSocket API（useWebSocket hook）

### 5.5 AI 模型接入

- **DeepSeek**: deepseek-chat / deepseek-reasoner / deepseek-v4-flash / deepseek-v4-pro
- **火山引擎豆包**: doubao-seed-2.0-pro
- 模型配置存储在 `agent-service/llm.config.json`，可通过设置页面动态修改
- MCP Pool: 4 个客户端连接池，支持工具并发调用

---

## 六、核心业务流程

### 6.1 订单履约分析流程

```
1. 用户在首页筛选/选择订单
2. 点击「加入对话」将订单加入 AI 上下文
3. 在聊天面板选择技能（或自动分配）
4. 输入自然语言问题（如"分析这两个订单的履约状态，生成可视化看板"）
5. AI Agent 通过 WebSocket 接收消息
6. Agent 调用 LLM 推理，可能调用工具获取数据
7. Agent 创建任务列表分步执行
8. 分析完成后发送 a2ui_surface 消息
9. 前端自动跳转 /a2ui 页面渲染看板
10. 分析结果持久化到 analysis_tasks 表
11. 用户可在历史分析中回溯
```

### 6.2 异常订单处理流程

```
1. AI 分析识别异常订单（签收率低/发货滞后/逾期等）
2. 生成问题卡片（problem_cards），按类别分组
3. 自动生成待办事项（todos）指派责任人
4. 对决策类问题生成多方案供人工选择
5. 通过通知渠道分发待办通知
6. 责任人在任务列表查看并处理
7. 完成后状态更新为"已完成"
```

### 6.3 物料齐套检查流程

```
1. 用户选择合同/设备
2. 调用「预缺分析与齐套检查助手」技能
3. Agent 查询 biz_materials 计算 current_stock + in_transit vs required_qty
4. 生成供需平衡报表
5. 识别短缺物料，生成采购建议
6. A2UI 看板展示齐套状态和趋势图
```

---

## 七、非功能性需求

### 7.1 性能要求

- 订单列表分页加载（支持 10/20/50/100 条每页）
- SQLite WAL 模式支持并发读写
- WebSocket 长连接复用，避免频繁握手

### 7.2 安全要求

- 密码 SHA256 哈希存储
- JWT Token 认证
- 路由级别的登录保护（ProtectedLayout）
- API 级别的认证中间件（authMiddleware）
- RBAC 精细化权限控制

### 7.3 可扩展性

- 技能系统支持热加载新技能
- 模型供应商可插拔配置
- MCP 协议支持工具扩展
- 通知渠道支持新增类型

---

## 八、术语表

| 术语 | 英文 | 说明 |
|------|------|------|
| 履约 | Fulfillment | 订单从签订到交付的完整执行过程 |
| 齐套 | Kit-complete | 生产所需全部物料准备就绪 |
| 签收率 | Receipt Ratio | 客户已签收量 / 总发货量 |
| 发货率 | Shipment Ratio | 已发货量 / 订单总量 |
| BOM | Bill of Materials | 物料清单：合同→设备→包→物料 |
| A2UI | Agent-to-UI | AI 智能体直接生成前端界面的协议 |
| MCP | Model Context Protocol | 模型上下文协议，用于工具调用 |
| 控制塔 | Control Tower | 供应链可视化监控中心的概念隐喻 |
